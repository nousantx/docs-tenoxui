import Code from "@code";
import DocLink from "@nav";

# Pseudo Class

In tenoxui, there's only two supported pseudo class (currently). This is `hover:` and `focus:`.

## Hover Effect

You can use `hover` effect by adding `hover:` prefix before the class name, like this :

<Code
  lang="html"
  className="my-1rem"
  codeOnly
>{`<div class="... bg-red hover:bg-blue"></div>`}</Code>

## Focus Effect

You can add `focus` effect by adding `focus:` prefix before the class name, like this :

<Code
  lang="html"
  className="my-1rem"
  codeOnly
>{`<button class="... bg-red focus:bg-blue"></button>`}</Code>

<DocLink
  prev="/docs/classes"
  prevIcon="style"
  next="/docs/classes/background"
  nextIcon="wallpaper"
/>